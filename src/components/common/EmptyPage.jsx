const EmptyPage = ({ children }) => {
    return (
        <div className="w-full h-25 flex justify-center items-center text-2xl">
            {children}
        </div>
    );
};

export default EmptyPage;
